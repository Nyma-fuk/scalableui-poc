# Dynamic Workspace Variant

`dynamic-workspace` is the current main ScalableUI HMI PoC. It avoids fixed XML slots and lets the runtime model create, remove, move, resize, and assign panels.

See `docs/hmi_spec_ja.md` and the top-level `docs/dynamic_workspace_notes_ja.md` for architecture, constraints, and validation history.
